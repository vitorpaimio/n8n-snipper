/** Handles de saída do nó IF (ramo escolhido em `ifBranchOutcome` na edição). */
export const IF_TRUE_HANDLE_ID = "if-true";
export const IF_FALSE_HANDLE_ID = "if-false";

export type IfBranchOutcome = "true" | "false";
